package main
import "fmt"
var array []int
func marge_sort(st int, end int, cnt, k) int {
	if(array[])
}
func main(){
	var n, k int
	fmt.Scanln(&n, &k)
	for i := 0; i < n; i++{
		var a int
		fmt.Scan(&a)
		array.add(a)
	}
	merge_sort(0, n, 0, k)
}